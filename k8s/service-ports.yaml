apiVersion: v1
kind: Service
metadata:
  name: grpc-server-external
  namespace: monitoring
spec:
  type: LoadBalancer
  selector:
    app: grpc-server
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
